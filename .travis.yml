language: node_js
sudo: required
before_script:
  - npm install -g polymer-cli
  - npm ci
node_js: stable
addons:
  firefox: latest
  apt:
    sources:
      - google-chrome
    packages:
      - google-chrome-stable
  sauce_connect: true
script:
  - xvfb-run npm test
dist: trusty
